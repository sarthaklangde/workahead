import DemoClient from "./DemoClient";

export const metadata = {
  title: "Workahead | organizational-memory-ssl",
  description:
    "Explore how Workahead turns a messy PRD into a clear plan, connected tickets, and visible agent execution.",
};

export default function DemoPage() {
  return <DemoClient />;
}
