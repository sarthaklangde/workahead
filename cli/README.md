# Workahead CLI

The CLI will be the local entry point for opening and refreshing Workahead sessions.

Planned commands:

```bash
workahead init
workahead open ./docs/prd.md
workahead refresh
workahead status
```

The CLI will hand off execution to the developer's existing Codex or Claude Code workflow. It
will not become a replacement agent.
