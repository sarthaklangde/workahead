# Organizational Memory example

`PRD.md` is a deliberately dense product requirements document used to test Workahead's ability
to turn messy context into a readable plan, tickets, and execution state.

It is example input, not Workahead's own product specification.
